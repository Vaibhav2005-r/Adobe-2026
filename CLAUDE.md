See @docs/build-plan.md for the full 10-day plan, rubric strategy, report schema, and risk/cut list. Read it before doing any work — it is the source of truth for scope and architecture decisions.

# Project: Brand AI Readiness Audit (Adobe Hackathon Round 3)

10-day take-home, Fri 28 Aug → Sun 6 Sep 2026. Deliverable: a Claude Skills marketplace (`brand-ai-readiness-audit/`) that audits a website's visibility to AI assistants.

## The thesis (never lose sight of this)

Don't audit the page — simulate the retrieval pipeline an AI assistant runs, and report the exact stage where the brand falls out: REACH → RENDER → EXTRACT → RETRIEVE → CITE → ARRIVE. Every finding must be stage-localised and artifact-backed (URL + status + selector/byte-offset + literal extracted strings). No artifact, no finding.

## Hard constraints (non-negotiable, check before every commit)

- ≤50 MB zip, no bundled model weights
- <5 minute runtime per audit (hard watchdog with a degradation ladder — record degradations in the report, never fail silently)
- Fully deterministic: same site in → same report out (three runs, byte-identical modulo `audited_at`)
- Read-only, robots-respecting, no auth, no writes to the audited site
- Portable: must run on a judge's bare machine — every optional dependency (Playwright) needs a stdlib/graceful-skip fallback

## Repo layout

```
brand-ai-readiness-audit/
├── marketplace.json          # exactly one entrypoint
├── README.md                 # thesis, composition contract, eval results
└── skills/
    ├── ai-visibility-orchestrator/   ENTRYPOINT — pipeline driver, report assembly
    ├── crawl-reach-audit/            ① REACH
    ├── render-gap-audit/             ② RENDER — dual-fetch differential
    ├── extractability-audit/         ③ EXTRACT
    ├── retrieval-simulation/         ④ RETRIEVE — crown jewel, BM25 answerability probe
    ├── trust-corroboration-audit/    ⑤ CITE
    ├── arrival-engagement-audit/     ⑥ ARRIVE
    └── finding-verification/         cross-cutting falsification pass
```

Each stage skill reads a shared `run_context` and writes `StageResult{findings[], artifacts[], corpus_delta, metrics}`. `retrieval-simulation` only sees the corpus that survived stages ① and ②. This gating is the composition story — don't break it by letting stages read the raw crawl directly.

## Tech stack (see build-plan for full rationale)

Python 3.11+, `httpx` (async), `protego` (robots), `playwright` (optional, stage ② only), `selectolax`/`lxml`, `trafilatura` (main-content extraction, identical on both fetch paths), `extruct` (structured data), hand-rolled BM25 (~120 LOC, no embeddings — deliberate: deterministic, no model weights, no API key). Pydantic v2 models generate `report_schema.json` — models are the source of truth, never hand-edit the schema.

## Working rules

- **Every rule must state a mechanism**, not just a symptom, before it enters the rule pack (`references/taxonomy.md`). If a fix only helps one site, it's overfitting — delete it.
- **Falsification before shipping a finding**: re-fetch, check reproducibility, look for contradicting signals, check sample adequacy. Findings that fail get demoted to `observations`, never silently dropped.
- Confidence/severity are computed by the deterministic `severity = f(stage, blast_radius, confidence)` function in `references/severity-model.md` — never hand-assigned.
- LLM usage is confined to two schema-constrained, temperature-0 calls (answerability classification, suggested-action prose) with deterministic fallbacks. Everything else is deterministic code.
- Validate every skill folder with `skills-ref validate` before committing; CI runs it on all 8 + manifest lint.

## Current status

**Project complete — all 10 days shipped.** Day 10 (Sun 6 Sep): package & narrative — done. The real README replaces the Day 2 placeholder (thesis, composition contract, the published confusion matrix, a corrected "fully deterministic, zero LLM calls anywhere" statement — verified by grep before writing it down, not assumed from the build plan's own aspiration, which had reserved two LLM touchpoints that ended up implemented as deterministic code instead). Every `SKILL.md` was already under the 500-line hygiene target with no retrofitting needed. The Day 10 DoD was met literally, not just claimed: zip built (216 KB), extracted into a directory that never touched the dev `.venv`, installed into a brand-new virtualenv with zero pre-existing state, and run end-to-end using only the README's own Quickstart command — produced a valid report in 3.2s, with the full test suite also passing in that same clean environment (155 passed, 1 correctly-skipped Playwright module, confirming the bare-machine story is real). A committed sample report (`sample-report/`, allbirds.com) uses `--skip-render` deliberately — the configuration most graders will actually experience, not the one that looks most complete. Two more stale-doc bugs caught in `taxonomy.md`'s own footer while touching it (a "33 entries" arithmetic slip that was actually 31, and a "Day 8 remaining" line three days out of date). 161 tests passing throughout. **The `marketplace.json` format assumption is now resolved** (post-Day-10 addendum): the brief itself remains unavailable (confirmed absent from the context folder, git history, and the GitHub remote), so rather than keep guessing, the published Claude Code plugin-marketplace spec was checked directly — and it turned out the root manifest's `metadata.entrypoint` key would be **rejected** by a strict validator, since the official schema puts the file at `.claude-plugin/marketplace.json` and defines `metadata` as `additionalProperties: false`. Both conventions now ship side by side, with `lint_marketplace.py` enforcing an anti-drift invariant between them. The same pass also corrected a real factual overstatement in the `TRUST-008` rule pack and its shipped `impact_mechanism` (the KDD 2024 GEO study says "up to 40%", not the "~+20-40% relative" we had claimed), and added a researched competitive-positioning section to the README — which deliberately *downgrades* two of our own novelty claims to what the evidence actually supports. See `docs/progress.md` for the full ten-day accounting plus that addendum.

**Post-completion hardening (addenda 3 and 4).** Auditing `openai.com` surfaced a "blocked vs. absent" conflation — a 403 is a *completed* HTTP transaction, so downstream stages were reading WAF block pages as content; every stage now publishes `pages_examined` and a stage that ran but examined nothing reports `skipped`, not `pass`. Then an external issues report was triaged claim by claim (three of its 25 didn't reproduce, one reproduced through a different mechanism than alleged) and produced seven confirmed fixes: malformed JSON-LD no longer crashes the audit, unparseable IDN hostnames no longer end the crawl, namespace-less and gzipped sitemaps are parsed instead of silently discarding a whole site, `EXTRACT-003` no longer masks heading skips after the first deep heading, `TRUST-006` aggregates across the corpus instead of returning on its first hit, and `ENGAGE-002` no longer accepts a generic token like "co" as proof the brand was named. The largest find was the **English-only pipeline**, which was measured rather than assumed: a deliberately well-built German fixture audited as 15/18 queries unanswerable with a false headline, a false `CHUNK-001`, a false `ENGAGE-005` and five false recommendations. The answerability probe and `ENGAGE-005` are now suppressed when the corpus *declares* a language the bundled lexicons don't cover — the same contract stage ② already honours for a missing Playwright: skip the measurement, suppress the findings, record the degradation, never guess. That fixture is now the sixth clean control. **227 tests passing**; confusion matrix unchanged at precision 1.00 / recall 1.00 / FP-rate 0.00, now over 13 certified-clean stage checks; allbirds.com re-audited live at the sample report's own parameters with identical output.

**Compliance pass against the real handout (addendum 6).** The Round 3 handout PDF became available for the first time; every prior session had worked from `docs/build-plan.md`'s summary of it, and addendum 1 had flagged the manifest format as an open assumption. The handout settled it, and the guess was wrong: it states plainly that the multi-skill marketplace format is *the contest's own convention* and gives it verbatim — `name`, `version`, and a `skills` array of `{id, path, entrypoint}` with exactly one `entrypoint: true`. What shipped was the **Claude Code plugin schema** instead (`owner`/`metadata`/`plugins[]`), which has no `skills[].entrypoint` field at all, so a grader checking "exactly one entrypoint" against their own convention would have found none. `marketplace.json` now matches the handout exactly, and `.claude-plugin/marketplace.json` — addendum 1's hedge against not knowing which convention applied — is **deleted**, since a hedge stops being risk reduction the moment the source document exists. `lint_marketplace.py` was rewritten to enforce the handout's convention, each check traceable to a line in it. Also closed: `license` was on 1 skill of 8 (now all 8), `allowed-tools` was on none (the handout says "Declare any allowed-tools"), the `SKILL.md` files didn't use the handout's prescribed `When to use` / `Inputs` / `Procedure` / `Output` sections (all 8 now do, substance folded in rather than rewritten), and ~two dozen comments pointed at `docs/build-plan.md` / `docs/progress.md` / `CLAUDE.md` — all of which live at the *repo* root, outside the zip, so every one was a broken pointer in a grader's extracted copy. Versions unified at `1.0.0`. Verified the way it will actually be received: built the zip (272 KB), extracted it somewhere that never touched the dev `.venv`, installed into a fresh virtualenv, and from that copy alone — lint clean, all 8 skills valid, a report in 3.8s, **230 passed / 1 skipped** (the Playwright module, correctly skipped on a bare machine).

**External 196-site sweep (addendum 7).** A 200-site QA campaign plus two verification passes arrived; every claim was re-checked against the code and live sites. Five items were already fixed (addenda 3-4). The "MAJOR" sitemap finding was **misdiagnosed** — `python.org`/`apache.org`/`react.dev`/`mit.edu` 404 on `/sitemap.xml` and declare no `Sitemap:` line, so their 1-page crawls were the documented fallback working, not the namespace/gzip bug. But the symptom was real and far larger: **39 of 131 completed audits (30%) crawled exactly one page**. Fixed with `discover_links_from_homepage` — a one-level, homepage-only, robots-checked, same-host, deterministically-sorted seed list for the existing sampler (python.org 1 → 65 URLs; a full audit now crawls 12 pages, not 1). Three confirmed false-positive classes fixed: `EXTRACT-003` reported "no `<h1>`" on python.org, which ships five in a hero carousel that `trafilatura` strips as boilerplate (the zero-h1 check now consults the raw DOM, without falling back to *counting* raw-DOM h1s, which would just swap in a "5 h1 tags" FP); `TRUST-008` stripped tags with a regex that keeps `<script>`/`<style>` *contents*, counting CSS lengths and script constants as claims — 27 "facts" from python.org's markup vs 1 from its visible text, which is why it fired on 101 of 196 sites; and even on visible text a *statistic* is a claim, not a number, so counting is now restricted to percentages and currency amounts (postgresql.org's eleven "statistics" were all version strings — `fastapi.tiangolo.com` still fires, correctly, on real unattributed "200% to 300%" claims the report had miscounted as noise). Also: ISO 4217 currency codes now extracted, a phantom-numeric bug fixed (`₹3,999` left a stray `999`), and `ENGAGE-004` downgraded to `low` confidence. **251 tests**; matrix unchanged at 1.00 / 1.00 / 0.00; three runs byte-identical; handout compliance from addendum 6 untouched (no new dependency, no manifest change, still read-only and robots-respecting).

**Auditing a page rather than a site (addendum 8).** Re-ran `https://openai.com/index/gpt-6-astra/` to confirm addendum 3's fixes held (they did) and found two more. **The requested page was never audited** — the site argument was only ever a domain seed, so a deep URL drew fifteen *other* sitemap pages and never the one asked about; `requested_page()` now separates a site-level request from a page-level one, and `stratified_sample` takes a `pinned` URL that is always first and never displaced by `--max-pages` (robots/sitemap/`llms.txt` discovery uses the origin, where those files live; a bare domain pins nothing and is byte-identical to before). **And the report contradicted itself**: the funnel table said `④ RETRIEVE — skipped` while the summary beneath it read "18 unretrievable (of 18 simulated buyer-intent queries)" — BM25 over an empty index returns nothing for every query, and the classifier recorded that as a measured verdict on content nothing had fetched. Addendum 3 fixed everything that *emitted a finding or recommendation* from an empty corpus; the matrix emits neither, which is why it survived — and it is the most quotable number in the report. **255 tests**; matrix 1.00 / 1.00 / 0.00; compliance untouched.

**A full six-stage run on an unblocked site (addendum 9).** Every recent live run had been against a WAF-blocked target, exercising only stage 1. A 25-page audit of `ghost.org` ran all six — and produced two wrong outputs, both from the same unimplemented piece of the build plan. **The detected brand was "Ghost Resources"**, so all 18 buyer-intent queries asked about a company that does not exist: `detect_entity` ranked a JSON-LD `Organization` name from *any* page above the homepage's own `<title>`, and ghost.org publishes one only on `/resources/`. That is the same hijack the docstring already documented for the title path (allbirds.com's "Design System", Day 6), arriving through the JSON-LD door — fixed for one door and not the other. Precedence is now homepage-first throughout. **And the report recommended publishing a pricing page to a company that has one** — `/pricing/` was in the sitemap, never sampled. Root cause for both: `stratified_sample` was never stratified; the docstring had said since Day 3 that page classification was "a stage-1 concern layered on top ... once page classification exists", and the note outlived the excuse. Now genuinely stratified — named URL, homepage, one page per pricing/contact/about/docs/product class, then hash rank — plus the origin is always a sitemap candidate (ghost.org's sitemap index yields 500 theme URLs before `/`, so the homepage wasn't unlucky, it was absent). A uniform sample estimates proportions; it does not find the specific pages a buyer asks about, and the probe asks about pricing and contact *by name*. Result: entity "Ghost", ungrounded 5 → 3, both false recommendations gone. **265 tests**; three runs byte-identical; matrix 1.00 / 1.00 / 0.00; compliance untouched.

## Commands

- Setup: `cd brand-ai-readiness-audit && python3 -m venv .venv && source .venv/bin/activate && pip install -e ".[dev]"` (Windows: `.venv\Scripts\activate` instead of `source .venv/bin/activate`; run `skills-ref` as `npx.cmd --yes skills-ref validate <dir>` or via `cmd /c`, since PowerShell's default execution policy blocks the `npx.ps1` shim)
- Run an audit: `python skills/ai-visibility-orchestrator/scripts/run_audit.py <site> [--max-pages 40] [--out report.json]` (also writes `report.html` + `report.md` alongside it)
- Validate skills: `for d in skills/*/; do npx --yes skills-ref validate "$d"; done`
- Manifest lint: `python skills/ai-visibility-orchestrator/scripts/lint_marketplace.py`
- Regenerate report schema after editing models: `python skills/ai-visibility-orchestrator/scripts/gen_schema.py`
- Tests (smoke + determinism, local fixture, no network): `python -m pytest tests/ -v`
- Run fixture eval / confusion matrix: `python scripts/eval_fixtures.py [--out results.md]`
