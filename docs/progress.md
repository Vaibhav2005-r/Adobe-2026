# Progress Log

Tracks DoD completion against `docs/build-plan.md` Part 7, one entry per day. Update as each day's DoD is met so a fresh session can pick up where the last one left off.

## Day 1 — Fri 28 Aug · Field research + taxonomy
**Status:** core pass done across 2 sessions; corpus breadth is the remaining open item

- [x] Wild corpus assembled — 12 sites given the full treatment (live-assistant query + technical diagnosis), 3 more screened for RENDER only and found clean. Close to, still just under, the 10–15 floor; see Notes.
- [x] Each site: asked a live assistant (Claude's web search, used as the "live assistant" proxy — see Notes) a buyer question about the brand, recorded cited/misrepresented/absent, then manually diagnosed why via `curl` (robots.txt, raw HTML, headers, JSON-LD) plus a real headless-browser render for the two confirmed RENDER cases
- [x] Defect taxonomy at `brand-ai-readiness-audit/skills/ai-visibility-orchestrator/references/taxonomy.md` — 5 shipped findings (3 REACH, 1 RENDER, 1 cross-site TRUST pattern), 2 explicit observations pending falsification, 1 low-severity note, 3 positive controls. **Not** frozen at ~30 — see Notes on why padding was rejected.
- [x] Every taxonomy entry states a mechanism, not just a symptom (self-checked against the admission rule while writing each one)

**DoD status:** substantially met. Mechanism-first discipline holds for every entry. RENDER-00x — previously the biggest gap — now has two confirmed, evidence-complete cases with a full dual-fetch (curl-raw vs. headless-rendered) comparison. Site count (12) and entry count (8, not ~30) are still short of the plan's targets but each entry is load-bearing, not padding.

**Notes:**

*Methodology:* used Claude's own web-search tool as the "live assistant" being probed (rather than separately driving ChatGPT/Perplexity UIs, which would have needed logins/rate-limit handling out of scope for a non-interactive session) — reasonable as a first-pass proxy since it's a real retrieval-and-cite pipeline, not a simulation, but worth re-running key queries through 1–2 other assistants before the taxonomy is trusted as generalizing across assistants, not just this one's retrieval quirks.

*Corpus (12 fully diagnosed):* stripe.com (SaaS/fintech), linear.app (Next.js SaaS), allbirds.com (Shopify DTC), nytimes.com (news, explicit AI-bot blocker), notion.so→notion.com (JS-heavy PaaS), docs.python.org (static docs, control), brittanychiang.com (single-page portfolio, Next.js SSG), theverge.com (news/tech, open robots but citation gap), zalando.de (non-English/German retailer), **docsify.js.org** (JS-only docs generator — confirmed RENDER case), **app.uniswap.org** (JS-only DeFi app shell — confirmed RENDER case), **curve.finance** (WAF/bot-challenge block found while chasing the RENDER lead — confirmed REACH case). Plus 3 screened for RENDER only and found SSR-clean (framer.com, webflow.com, figma.com).

*Finding the RENDER cases:* the first pass tested big-brand marketing sites (Framer, Webflow, Figma, Linear) and found all of them SSR-clean — a real negative result, not a miss, but it left the plan's highest-ROI feature (dual-fetch differential) without a positive example. Redirecting the search toward **framework-driven doc generators that are explicitly client-side-only by design** (docsify) and **application shells never built with crawlability in mind** (a DeFi trading UI, Uniswap) found two textbook cases on the first two tries: both ship a literal empty `<div id="app">`/`<div id="root">` in the raw HTTP response. The lesson for the detector: targeting matters more than volume — "is this a marketing site" is the wrong filter; "is this a JS-framework app-shell with no build/SSG step" is closer to the real signal.

*Why not 10–15 sites / ~30 entries yet:* same discipline as the first session — quality of diagnosis over site count. Each RENDER case required a curl-raw fetch *and* a real headless-browser render of the *same URL* to make the before/after comparison airtight, which is slower than a single probe. TRUST-002 and TRUST-003 remain explicit single-sample observations, not promoted to findings.

*Biggest surprises this session:* (1) both RENDER cases had a clean **citation consequence**, not just a technical gap — docsify.js.org's own render gap handed its citation to `github.com/docsifyjs/docsify` (GitHub server-renders the README) instead, and Uniswap's gap was partly absorbed by its own render-friendly sibling subdomains (`support.`, `developers.`) rather than the app itself. This is exactly the "artifact-backed, stage-localized" story the audit is meant to tell. (2) curve.finance turned up by accident while chasing a RENDER lead: its WAF blocks *all* UAs including a plain browser string, and even blocks `/robots.txt` itself — a stronger failure than REACH-001's explicit `Disallow`, because the crawler can't even learn what's permitted. Worth remembering as a lesson: some of the best findings come from following a technical trail, not from working strictly one query at a time down a fixed list.

*Next session should:* (1) push corpus to 15 with categories still uncovered (a confirmed WordPress-powered local business; a native-language control query for the Zalando language-mismatch observation); (2) re-run TRUST-002 and TRUST-003 with a second query each to either promote or drop them; (3) run a live-citation query for curve.finance (REACH-003 currently has no citation-test corroboration, unlike every other finding); (4) start Day 2 scaffolding once corpus/taxonomy coverage feels sufficient, rather than blocking Day 2 entirely on hitting 30.

---

## Day 2 — Sat 29 Aug · Contracts + scaffolding
**Status:** done

- [x] `marketplace.json` with exactly one entrypoint (`ai-visibility-orchestrator`); all 8 skill folders with valid stub `SKILL.md` (name matches folder, only legal frontmatter keys used -- confirmed against the actual `skills-ref` validator source, not guessed: `name`, `description`, `license`, `allowed-tools`, `metadata`, `compatibility`)
- [x] `skills-ref validate` green on all 8 (via `npx skills-ref`, real Anthropic package, not a stand-in)
- [x] Pydantic v2 models (`src/brand_audit/models.py`): `Finding`, `Artifact`, `StageResult`, `AuditReport`, plus `Scope`/`Verification`/`SuggestedAction`/`RunManifest`/`Summary`/etc.; `report_schema.json` generated from them via `scripts/gen_schema.py` (CI checks for drift)
- [x] Crawl core (`src/brand_audit/crawl.py`, `fetch.py`, `artifact_store.py`): `protego`-based robots/AI-UA policy, sitemap discovery (index + urlset), deterministic stratified sampler (seeded by `sha256(domain)`, time-independent), `BudgetManager` with an ordered degradation ladder, concurrent polite fetch, artifact persistence to `runs/<domain>/artifacts/`
- [x] CI wired (`.github/workflows/ci.yml` at the repo root): `skills-ref validate` x8, manifest lint, schema-drift check, pytest smoke suite -- all run against a local fixture server, no live network dependency
- [x] **DoD met:** `run_audit.py` crawls a real site (tested against docs.python.org and a local fixture) and emits a schema-valid report with zero findings. Confirmed deterministic: two runs, byte-identical modulo `audited_at`/`duration_s` (`tests/test_smoke.py::test_determinism_across_runs`).

**Notes:**

*skills-ref, for real:* rather than guess at the "6 legal frontmatter keys" CLAUDE.md mentions, read the actual validator source (`npx skills-ref` pulls the real `skills-ref` npm package) at `validator.js` -- confirmed the allowed set, the name-format rules (lowercase, <=64 chars, no leading/trailing/double hyphens, must match directory name after NFKC normalization), and the description length cap (<=1024 chars). This is why every SKILL.md passed validation on the first real run, not after several rounds of guessing.

*marketplace.json format assumption, flagged explicitly:* `skills-ref` has no marketplace-level schema of its own -- "marketplace.json, exactly one entrypoint" is the hackathon brief's own convention, and the original brief document isn't available in this session, only CLAUDE.md's summary of it. Modeled the file on Anthropic's own real `.claude-plugin/marketplace.json` (fetched from `anthropics/skills` on GitHub) -- `name`/`owner`/`metadata`/`plugins[].skills`. "Exactly one entrypoint" is expressed two ways: (1) a custom `metadata.entrypoint` field naming `ai-visibility-orchestrator`, checked by `lint_marketplace.py`; (2) every other skill's `description` is worded as "internal pipeline stage ... not meant to be invoked directly" so it wouldn't get triggered by a generic user request even though it's a technically valid, independently-invocable skill. Worth double-checking against the actual brief if it turns up.

*Where shared code lives:* the build plan's file tree only shows `scripts/` explicitly under `ai-visibility-orchestrator/`; it doesn't say where cross-stage code (Pydantic models, crawl core) should live. Put it in `src/brand_audit/` as a proper installable package (`pyproject.toml` + `pip install -e .`) rather than duplicating models across 8 skill folders -- every stage's future `scripts/` imports from it. This keeps each skill's own `scripts/` thin and stage-specific, which is what the composition story needs (see `references/composition.md`).

*Determinism bug caught by the smoke test:* first local-fixture run showed `0/1 sampled pages reachable` -- turned out `run_audit.py`'s site-normalization always prepends `https://` when no scheme is given, which silently broke against the plain-HTTP local fixture server. Not a real bug for actual audit targets (real sites serve HTTPS), but worth remembering: `run_audit.py http://localhost:PORT` needs the explicit scheme for local/test use.

*Next session should:* start Day 3 (stage ① REACH detectors + stage ② RENDER dual-fetch differential). The REACH detector layer has real material to work from already -- `REACH-001`/`REACH-002`/`REACH-003` in `references/taxonomy.md` are three field-verified mechanisms ready to become actual `Finding`-emitting code, not hypothetical rules. Same for `RENDER-001`. This should make Day 3 mostly translation (taxonomy entry -> detector) rather than fresh discovery.

## Day 3 — Sun 30 Aug · Stage ① REACH + Stage ② RENDER
**Status:** done

- [x] `crawl-reach-audit` detectors (`skills/crawl-reach-audit/scripts/detect.py`): `REACH-001` (named AI-UA robots block), `REACH-002` (locale-redirect empty body), `REACH-003` (WAF/bot-challenge block) translated directly from Day 1 field evidence; `REACH-004` (soft-404), `REACH-005` (canonical integrity), `REACH-006` (sitemap health) engineering-derived from the build plan's own Day 3 bullet list. All severity-computed via the new `src/brand_audit/severity.py` (the `f(stage, blast_radius, confidence)` function from `severity-model.md` -- implemented for real, not deferred again).
- [x] `render-gap-audit`'s dual-fetch differential (`skills/render-gap-audit/scripts/render_detect.py`): fact-level diff (currency/numeric/date/contact, regex-based -- "entity" extraction deliberately left undone rather than faked, see Notes), noise suppression (today's date, hex/CSRF-looking tokens), primary empty-shell signal plus a secondary per-fact-category signal for partial render gaps.
- [x] **DoD met, as an executable test, not just eyeballed:** `tests/test_render_gap.py` builds a `tests/fixtures/js-only-price` fixture (empty `<div id="app">` shell, JS injects the content on load) and asserts the differ flags it (`RENDER-001`, critical, high confidence) while `tests/fixtures/clean-control` stays silent.
- [x] `run_audit.py` now runs both stages end-to-end; render stage gracefully skips (records `render_stage_skipped_no_playwright` degradation, `ai_readiness.render: skipped`) when playwright isn't installed, never guesses.
- [x] Test suite grown to 14 tests (unit tests for every REACH detector against synthetic fixtures, not just the two end-to-end fixture sites); CI split into two jobs so the base job stays representative of a judge's bare machine (no playwright) while a second job proves the render detector itself works.

**Notes:**

*Real-site validation, not just fixtures:* re-ran the pipeline against nytimes.com and curve.finance -- the two real sites Day 1 used to hand-derive `REACH-001` and `REACH-003` -- to check the automated detectors reproduce the by-hand findings. `REACH-001` caught nytimes.com correctly but initially under-scored it (`medium` instead of `critical`) because the site blocks 11 of 12 named bots, not literally all 12 (it allows plain `Applebot` while blocking `Applebot-Extended` -- Apple's own real distinction between its search-indexing and AI-training bots). Fixed by changing the site-wide threshold from exact-equality to `>=90%`, which is more robust and still doesn't fire on a genuinely partial block. `REACH-003` did **not** reproduce against curve.finance this time -- its WAF let 2 of 3 probe UAs through, unlike Day 1's all-3-blocked result. Investigated rather than dismissed: the detector's own gating (`skip if robots.txt is readable`) correctly stayed silent rather than guessing, and the live inconsistency itself is a useful data point -- WAF/bot-challenge behavior is adaptive and not fully reproducible run-to-run, which is exactly why `finding-verification`'s re-fetch/reproduction check (Day 8) exists. Recorded here rather than treated as a bug to hide.

*A real module-naming bug, caught before it shipped:* both `crawl-reach-audit` and `render-gap-audit` initially had a script named `detect.py`. Since both directories get added to `sys.path` by `run_audit.py`, Python's global module cache would have silently returned whichever one loaded first for both imports -- a real bug that unit tests alone wouldn't have caught (they only import one skill's detectors at a time) but the wired-up `run_audit.py` would have hit immediately. Renamed the render one to `render_detect.py`.

*"Entity" fact extraction, honestly skipped:* the build plan names five fact types for the differential (numeric/currency/date/entity/contact); only four are implemented. A real entity extractor needs an NER model, which conflicts with the project's own "no model weights" constraint, and a naive regex heuristic (e.g. "any capitalized multi-word phrase") would be noisy enough to undermine the "few false positives" goal. Documented as a deliberate gap in `render_detect.py`'s docstring rather than papered over with a bad heuristic.

*Severity function implemented now, not deferred to Day 8:* `severity-model.md` said the `f()` function was Day 8 work, but since Day 3's detectors needed to assign severity to real findings *today*, implementing the already-fully-specified decision table now (rather than hand-assigning severities inconsistently and reconciling later) was strictly less work. `src/brand_audit/severity.py` is the real implementation; Day 8 is now about the *merge/dedup* half of finding-verification, not severity itself.

*Next session should:* start Day 4 (stage ③ EXTRACT: `extruct` parse/validate, schema-vs-visible-text contradiction detection, semantic HTML integrity, facts-locked-in-images). The Allbirds control case from Day 1 field research (complete `ProductGroup`/`Offer` JSON-LD in raw HTML) is a ready-made positive-control fixture candidate.

### Post-Day-3 review pass (full-codebase audit, requested explicitly)

Re-read every file with fresh eyes against CLAUDE.md's hard constraints rather than trusting the code because I'd written it. Found and fixed six real issues, two of them meaningful:

1. **Robots.txt permission was parsed but never enforced (compliance gap).** `RobotsPolicy.allowed()` existed and was correct, but nothing in `run_audit.py` ever called it before fetching a sampled page -- every sampled URL got fetched regardless of `Disallow` rules, which directly violates the "read-only, robots-respecting" hard constraint. Fixed: sampled URLs are now filtered through `robots.allowed()` before any fetch. Verified end-to-end, not just by code inspection: `tests/test_robots_compliance.py` serves a fixture whose sitemap deliberately lists a page robots.txt disallows, then asserts no artifact anywhere in the run directory references that URL. Re-ran against nytimes.com afterward -- it now correctly fetches 0 of its sampled pages (all disallowed) while still detecting and reporting `REACH-001`, since that detection is a robots.txt rule lookup, not a fetch.
2. **The 5-minute watchdog was decorative.** `BudgetManager` was instantiated but `elapsed()`/`over_budget()`/`maybe_degrade()` were never called anywhere -- only the static `--max-pages`/`--max-render-pages` caps offered any real protection, with no adaptive mid-run bailout. Fixed: the REACH and RENDER stages are now wrapped in `asyncio.wait_for(budget.remaining())`; a timeout produces a valid, honestly-degraded report (`degradations: ["*_timed_out_budget_exhausted"]`) rather than running past the cap or crashing. Also added a proactive check (`MIN_RENDER_BUDGET_S`) so RENDER doesn't even start Chromium when there isn't enough budget left to be worth it. Verified with `tests/test_budget.py` (a near-zero budget produces a fast, degraded report, not a hang).
3. **`detect_sitemap_health` (REACH-006) was unreachable dead code.** `discover_sitemap_urls` always falls back to `[base_url]` when nothing is discovered, so the caller's `sitemap_reachable=bool(sitemap_urls)` was always `True` regardless of whether the declared sitemap actually worked -- the detector's condition could never be met. Fixed by having `discover_sitemap_urls` return `(urls, sitemap_fetch_ok)` as two separate signals. Caught with zero prior test coverage on this detector -- added `tests/test_reach_detectors.py::test_sitemap_health_*` while fixing it.
4. **`render_fetch` silently converted a render timeout/failure into `""`**, indistinguishable from "genuinely rendered to nothing." Real risk: a page whose render times out (persistent websockets/analytics never reaching `networkidle` -- curve.finance did exactly this during Day 1 field research) would produce a false *negative* rather than being flagged as unknown, which is backwards for a detector that's supposed to fail toward "flag it," not "miss it." Fixed: `render_fetch` now returns `None` on failure; the caller skips comparison for those pages rather than guessing either direction. Also switched Playwright's wait strategy from `networkidle` (fragile on real sites with persistent connections) to `load` + a short settle delay.
5. **`detect_canonical_issues` used a regex requiring `rel=` before `href=` in source order**, silently missing valid HTML with reversed attribute order. Rewritten using `selectolax` (already a project dependency) for real HTML parsing instead of a hand-rolled regex.
6. **`REACH-001` only ever checked the homepage path**, so a robots.txt rule scoped to a specific path (e.g. `Disallow: /products`) would be invisible to the detector even though it's a genuine, reportable defect. Fixed to check every sampled URL (a robots.txt rule lookup, not a fetch, so this is free) and made the site-wide/degrades classification two-dimensional (bot coverage x page coverage) so a path-scoped block doesn't get miscategorized as site-wide just because it fully blocks bots on that one path.

Smaller, lower-priority items noted but deliberately not fixed this pass (would be diminishing returns right now): `assemble_report`'s headline was picking `findings[0]` by list-concatenation order rather than the most severe finding -- fixed, this one was quick. `pages_crawled` in the run manifest now means "successfully fetched" rather than "attempted" -- also fixed, one-line change. Not fixed: the `today_iso` date-suppression in the fact-diff only matches exact ISO format, so a same-day "generated at" timestamp in a different format wouldn't be suppressed (low impact -- it's already a secondary, medium-confidence signal); render-page selection for `--max-render-pages` has no page-class prioritization yet (acceptable, page classification doesn't exist until later).

All 24 tests pass (10 new this pass); re-validated against docs.python.org (0 findings, clean) and nytimes.com (correctly still flags `REACH-001` critical, now with 0 pages actually fetched); determinism reconfirmed byte-identical. `skills-ref validate`, manifest lint, and schema-drift check all still green. Zip payload (excluding `.venv`/`node_modules`/caches) is ~324 KB, nowhere near the 50 MB cap.

**Plan-alignment assessment:** every hard constraint in CLAUDE.md is now actually enforced in code, not just documented -- the two compliance-critical gaps (robots-respecting, 5-minute watchdog) were real and are the main reason this review was worth doing before Day 4 added more surface area on top of an unenforced foundation. Detection accuracy and composition are on track per the rubric self-check in the build plan; determinism holds; the taxonomy's mechanism-first discipline held up under fresh scrutiny (no entry needed rewriting, only the code implementing REACH-001 needed to actually match what the taxonomy entry already said). No scope or architecture changes -- this was entirely a correctness pass.

## Day 4 — Mon 31 Aug · Stage ③ EXTRACT
**Status:** done

- [x] `extruct` parse (JSON-LD; microdata/RDFa parsed but not yet checked -- see Notes) + validation against a bundled offline schema.org subset (`assets/schema-subset.json`)
- [x] Schema↔visible-text contradiction detection (`EXTRACT-001`) -- both sides normalized to a comparable float before comparing, so formatting differences (`"199"` vs `"$199.00"`) don't false-positive as contradictions
- [x] Semantic HTML integrity: heading hierarchy (`EXTRACT-003`)
- [x] Facts-locked-in-images detection (`EXTRACT-004`), narrowly scoped to filename keywords rather than every alt-less image
- [x] **DoD met, as an executable test:** `tests/test_extractability.py::test_schema_contradiction_fixture_is_flagged_end_to_end` and `::test_clean_product_fixture_has_zero_false_positives` run the real pipeline against two fixtures (a JSON-LD/visible-text price mismatch; a matching-price, complete-properties, correct-hierarchy control) and assert the exact DoD wording.
- [x] Shared fact-extraction (currency/numeric/date/contact regex) promoted from `render-gap-audit` into `src/brand_audit/facts.py` so `extractability-audit` reuses it rather than duplicating -- also fixed the date-suppression format gap flagged in the prior review pass while it was there (see "Improvements" below).

**Notes:**

*A real false positive caught by testing against real sites, not just fixtures:* the first implementation of `EXTRACT-003`/`EXTRACT-004` scanned the *entire* page DOM for headings/images. Run against docs.python.org for validation (not required by the DoD, but cheap and the project's own Part 6 discipline says to do it), it flagged a "heading level skip" identically on all 8 sampled version pages. Traced it before assuming it was a real defect: every flagged `<h3>` lived inside `<nav class="menu">` or `<div class="sphinxsidebar">` -- Sphinx's own sidebar widgets ("Download", "Other resources"), nothing to do with the article's actual outline. Fixed by scoping both detectors to `trafilatura`'s main-content extraction (the same tool already used for the visible-text side of `EXTRACT-001`) instead of the raw page -- re-validated clean on the same 8 pages afterward, and confirmed the fix generalizes (not overfit to this one site) by also re-checking against a real Allbirds product page, which went from a spurious "3 `<h1>` tags" to correctly silent. That same re-check surfaced a *different*, genuine, low-severity finding on a sibling Allbirds page -- content sections marked up with real `<h2>`/`<h3>` tags but the product title itself never marked up as `<h1>` anywhere in main content -- which is exactly the kind of thing this detector exists to catch, so its surviving after the fix is a good sign, not a leftover bug.

*A near-repeat of Day 3's module-collision bug, caught before it shipped:* `extractability-audit`'s detector script was initially also named `detect.py`, which would have collided with `crawl-reach-audit`'s `detect.py` in `sys.path` exactly the way `render-gap-audit`'s did on Day 3. Renamed to `extract_detect.py` immediately, before wiring it into `run_audit.py` -- worth naming stage-skill scripts distinctly from the start now that the pattern's established, rather than re-discovering the same bug a third time on Day 5.

*Improvements considered from the prior review pass, both addressed:* (1) the `today_iso` date-suppression only matched exact ISO format -- fixed with `facts.today_variants()`, which generates every format the date regex itself can capture (US slash, month-name) so a same-day timestamp in any of those forms gets suppressed too; unit-tested in `tests/test_facts.py`. (2) render-page-selection prioritization by page-class -- reconsidered, still deliberately not done: page classification doesn't exist yet (no stage produces it), so "prioritize by page class" has no real signal to prioritize *by* right now. Forcing it in ahead of that dependency would be premature complexity, not a genuine improvement -- revisit once a stage actually classifies pages (retrieval-simulation, Day 5, is the first stage that plausibly needs this anyway).

*Scope boundaries, stated honestly rather than silently left out:* `extruct` parses microdata and RDFa alongside JSON-LD, but `EXTRACT-001`/`EXTRACT-002` only walk the `json-ld` result -- a page carrying the same facts via microdata *only* would currently look empty to these detectors. Table/`<dl>` structure checks and canvas/PDF fact detection (both named in the build plan's Day 4 bullet) aren't implemented -- `EXTRACT-004` covers only the `<img>`-filename-keyword case. None of this is hidden: `extractability-audit/SKILL.md`'s Status section lists exactly what's covered and what isn't.

*Next session should:* start Day 5 (stage ④ RETRIEVE, part 1 -- chunker, BM25 index, query template bank). This is the crown jewel and the plan's own schedule treats Days 5-6 as non-negotiable. The chunker's provenance-tracking requirement (back to URL + DOM position) pairs naturally with `EXTRACT-003`'s heading-hierarchy work already done -- the same main-content-scoped heading structure this stage now extracts is exactly what a chunker would want to segment along.

## Day 5 — Tue 1 Sep · Stage ④ RETRIEVE (part 1)
**Status:** done -- the bug-densest day so far, documented in full because the pattern matters more than any single fix

- [x] Chunker (`src/brand_audit/chunk.py`): 400-600 token target with overlap, boilerplate-stripped via `trafilatura`, provenance-tracked to URL + nearest heading + char offset.
- [x] BM25 index + `Retriever` protocol (`src/brand_audit/retrieval.py`): hand-rolled, deterministic, tie-broken explicitly by `(url, chunk_index)` rather than relying on incidental sort/insertion order.
- [x] Query template bank (`skills/retrieval-simulation/assets/query-templates.json`): 6 intent classes x 3 templates = 18 queries, deterministically expanded from the site's own detected entity -- 18 is a deliberate callback to the build plan's own report-schema example ("5 of 18 buyer-intent queries are UNANSWERABLE").
- [x] Entity detection (`skills/retrieval-simulation/scripts/retrieve_detect.py`): JSON-LD `Organization`/`LocalBusiness` first, then `<title>`, then `<h1>`, deterministic across dict ordering.
- [x] Answerability classification: deterministic (no LLM call this pass -- see Notes), checks every top-k result against a coverage-of-substantive-terms threshold, cross-checked against a concrete fact type where the intent has one (pricing -> currency, contact -> contact), with an "assembled across chunks" path for `PARTIAL`.
- [x] **DoD met, verbatim, as an executable test:** `tests/test_retrieve_stage.py::test_answerability_matrix_is_byte_identical_across_two_runs` runs `run_audit.py` twice against `tests/fixtures/retrieval-answerable` and asserts byte-identical reports modulo `audited_at`/`duration_s`.
- [x] Wired into `run_audit.py` as stage ④, gated per the composition contract (REACH survivors minus RENDER-proven-empty pages, not RENDER's own sampled subset -- see composition.md).

**Notes -- this day found more real bugs than Days 2-4 combined, across two categories:**

*Category 1: a genuinely broken selectolax assumption, latent since Day 4.* `tree.css("h1, h2, h3, p")` (a grouped CSS selector) does **not** return matches in document order -- it concatenates all matches of the first selector in the group, then the second, and so on. Confirmed directly: `<h2>B</h2><h1>A</h1><p>x</p><h3>C</h3>` returned `h1 A, h2 B, h3 C, p x`, not the true `h2 B, h1 A, p x, h3 C`. This had been silently wrong in `EXTRACT-003` (`extractability-audit`) since Day 4 -- undetected because every test case and the one real site checked against happened to have headings whose tag-grouped order was indistinguishable from true document order. Building the chunker's per-word heading attribution made it impossible to miss (every chunk came back tagged with the *last* heading in the document, regardless of position). Fixed in both places using `tree.body.iter()` / `tree.iter()`, which does preserve true order. **Lesson logged for future days:** grep for other `.css()` calls before trusting a similar pattern again -- checked, the only other two call sites (`img`, `link[rel="canonical"]`) are single-tag selectors, unaffected.

*Category 2: a chain of answerability-classifier bugs, each one masking the next.* Building a synthetic 3-page fixture (a real $89 price, a real contact email) and checking whether the classifier found them -- rather than trusting the code because it ran without raising -- surfaced, in order: (1) section headings weren't part of any chunk's *searchable text* at all, only tracked as metadata, so a "Contact" heading could never make a "how do I contact you" query match; fixed by prepending every distinct heading a chunk spans (not just the first) to its own text. (2) The entity's own name tokens ("Rowan Cast Iron Co.") dominated the coverage calculation since every query template bakes in `{brand}` but real pages rarely repeat the full company name next to a specific fact -- a real price and a real contact email both scored zero substantive coverage purely because of this; fixed by excluding entity tokens from the coverage calculation. (3) The same dilution problem again, from question-scaffolding words ("how", "does", "what") that appear in nearly every query template but never in answer text; expanded the BM25 stopword list to include them. (4) No stemming meant query "cost" never matched document "costs" -- added a minimal, deliberately conservative suffix-stripping rule (not a real linguistic stemmer, no dependency). (5) Even with all of the above fixed, the classifier only ever checked the single top-ranked BM25 result -- but BM25 ranks by raw score, which rewards rare terms regardless of *which* question they answer, so a correctly-retrieved pricing chunk ranked #2 behind an unrelated page that merely shared the brand name; fixed by checking every top-k result independently rather than just `results[0]`. Each of these was invisible in isolation (the pipeline never crashed, never looked obviously wrong) and only surfaced by asking "does this concrete, known-true fact actually get classified as answerable" against real content, repeatedly, until it did.

*A model-constraint crash, found by testing an edge case deliberately:* an empty AI-reachable corpus (a single-page site whose only page was excluded as a `RENDER-001` empty shell) crashed with a pydantic `ValidationError` -- the `CHUNK-001` summary finding tried to construct with zero artifacts, violating `Finding`'s own `min_length=1` constraint (the model-level enforcement of "no artifact, no finding" from Day 2). Fixed by skipping the finding (not fabricating an artifact) when there's no page left to cite -- the matrix itself still reports honestly (everything `UNRETRIEVABLE`), and `RENDER-001` already carries the real evidence for *why*.

*A real-site bug the fixtures couldn't have caught:* validating against docs.python.org (not required by the DoD, but the established habit from every prior day) surfaced the detected entity as "3.10.20 Documentation" instead of anything Python-related -- `detect_entity`'s homepage-preference logic required *exact string equality* between the CLI's site argument and a crawled URL, which real crawled URLs (with paths, trailing slashes) essentially never satisfy. Fixed to identify the homepage by normalized root path (`urlparse(url).path in ("", "/")`) instead. Confirmed working against the `retrieval-answerable` fixture (which has a real root page); docs.python.org's own sitemap turned out to only list 8 version-specific pages with no homepage entry at all, so that *particular* site still won't benefit from the fix -- a separate, smaller, honestly-noted limitation, not the bug that was fixed.

*Shared code promoted, not duplicated, twice more:* `src/brand_audit/facts.py`'s `extract_facts` (fact-check grounding) and a new `src/brand_audit/jsonld.py` (JSON-LD extraction, previously duplicated in `extract_detect.py`) are both now shared between `extractability-audit` and `retrieval-simulation` rather than copy-pasted a second time. Also caught, mid-refactor: an `Edit` with `replace_all=true` silently only replaced one of two occurrences of `_extract_json_ld(html)` -> `extract_json_ld(html)`, leaving a `NameError` that the test suite caught immediately -- a reminder to verify a replace_all edit's occurrence count rather than trust the flag name.

*LLM classification, deliberately deferred:* the build plan's tech-stack section names two LLM touchpoints (schema-constrained, temp-0, deterministic fallback) -- answerability classification is one of them -- but no specific day in the plan's own schedule assigns wiring it in, and full determinism is Day 5's explicit DoD. Shipped the deterministic classifier as the actual mechanism (matching "the deterministic fallback" language, not a placeholder standing in for it), not an LLM call. Worth revisiting only if the deterministic classifier's precision proves insufficient during Day 9's evaluation pass -- not before.

**Plan-alignment self-check:** every fix this pass came from either (a) real bugs a test caught, or (b) real bugs a live site surfaced -- none were guessed or spec-compliance busywork. The `selectolax` ordering bug in particular is the kind of thing that could easily have shipped all the way to Day 10 undetected if this stage's specific demands (per-word attribution) hadn't happened to expose it; worth remembering that a detector "passing its tests" is not the same claim as "the primitives it's built on are correct," and re-validating early code against a *new* stage's different demands is a legitimate way to find bugs, not just a Day-4-review-style special exercise.

**Next session should:** start Day 6 (stage ④ part 2 -- orphan-fact detector, cross-page-join detector, boilerplate-ratio scoring; stage ⑤ CITE -- entity anchoring, name-collision, freshness, description drift). The orphan-fact detector's "subject and value in different chunks" mechanism has a natural implementation hook already: `Chunk.section_heading` plus the fact-extraction primitives in `brand_audit.facts` are most of what's needed to check whether a chunk containing a price also contains something identifying *what* it's the price of.

## Day 6 — Wed 2 Sep · Stage ④ part 2 + Stage ⑤ CITE
**Status:** done

- [x] Orphan-fact detector (`CHUNK-002`): a chunk with a currency fact but no identifying subject (heading, or a proper-noun-like phrase) nearby.
- [x] Cross-page-join detector (`CHUNK-003`): a `PARTIAL` answerability outcome whose contributing chunks span more than one URL, not just one page's sections.
- [x] Boilerplate-ratio scoring (`CHUNK-004`): chunks diluted by cookie/legal/social-follow phrases past a threshold.
- [x] `trust-corroboration-audit` (stage ⑤ CITE): entity anchoring (`TRUST-005`), freshness/staleness (`TRUST-006`), description drift (`TRUST-007`), attribution/statistic density (`TRUST-008`). Deliberately did **not** implement a live name-collision web search -- the build plan's own cut list names it the first thing to cut if behind schedule, paired explicitly with "keep on-site entity anchoring" (`TRUST-005`); it's also a real tension with the project's determinism/portability constraints (live results, third-party network dependency), not just a scope-saving skip.
- [x] **DoD met:** all detection stages (①-⑤) wired into `run_audit.py`. Ran the full pipeline against an 11-site wild corpus (nytimes.com, docs.python.org, stripe.com, linear.app, allbirds.com, notion.com, brittanychiang.com, theverge.com, docsify.js.org, curve.finance, zalando.de) -- zero crashes, all exit 0, and every report's findings were spot-checked for sanity, not just absence of a stack trace (see Notes).

**Notes:**

*A new, real detector found and shipped the same day, via the wild-corpus sweep itself:* running the DoD's own "no crashing" check against allbirds.com surfaced something more interesting than a crash -- 0 of 8 sampled pages reachable, with no exception. Traced it: `robots.txt` is a completely standard, permissive Shopify robots.txt (no rule blocking any sampled URL), all 8 URLs passed this project's own robots-permission filter, and every one of them still returned HTTP 403 from the live fetch. That's a real, distinct mechanism from `REACH-003` (which requires `robots.txt` itself to be unreadable) -- here the declared policy is perfectly normal, and an infrastructure layer (WAF/bot-management) contradicts it anyway. Implemented as `REACH-007`, unit-tested, and confirmed catching the real allbirds.com case, all within the same session the DoD sweep that found it was run. This is the intended shape of Day 9's later "wild diversity sweep" discipline, just arriving three days early because the sweep needed to happen anyway to satisfy Day 6's own DoD.

*Determinism re-confirmed with two more stages in the mix:* ran the `retrieval-answerable` fixture through the full pipeline (now five active stages) twice and diffed -- still byte-identical modulo `audited_at`/`duration_s`. Nothing about CITE's detectors (pure regex/JSON-LD parsing, one parameterized reference date) introduced any non-determinism, but this was checked, not assumed.

*Same fixture-scoping issue as Day 5, same fix, made more future-proof this time:* `clean-control` and `schema-clean-product` again produced "new stage, existing fixture wasn't built for it" findings (`TRUST-005` fires on both, since neither fixture's Organization JSON-LD has a `sameAs` -- correctly, since neither was built to have one). Fixed the same way as Day 5 (scope the assertion to the stage the fixture actually certifies), but switched both from an *exclusion* list (`stage != "retrieve"`) to an *inclusion* list (`stage in {"reach","render","extract"}` / `stage == "extract"`) -- an exclusion list needs editing every time a new stage lands (this is the second time), while an inclusion list only needs editing if a fixture's own certification scope changes, which is rarer. Small design change, but worth doing once the pattern repeated rather than patching the symptom a third time on Day 7.

*A homepage-resolution bug prevented before it shipped, not after:* `trust-corroboration-audit`'s description-drift check needed the same "which crawled URL is the homepage" lookup that turned out to be a real bug in `retrieval-simulation` on Day 5 (exact string equality against the CLI's site argument, which real crawled URLs essentially never satisfy). Recognized the pattern before writing a second copy of either the bug or the fix, and promoted `find_homepage_url` into `src/brand_audit/crawl.py` as a shared function -- used fresh by `trust_detect.py` and refactored into `retrieve_detect.py` in place of its own Day-5 inline fix, so there's exactly one implementation of "resolve the homepage" in the codebase, not two copies that could drift apart.

*Scope note, stated honestly:* `TRUST-007` (description drift) checks meta description, JSON-LD description, and `og:description` -- the build plan's own bullet also names `<title>` and footer consistency, neither implemented yet. Noted in `trust-corroboration-audit/SKILL.md`'s Status section rather than left silently out.

*Next session should:* start Day 7 (stage ⑥ ARRIVE -- answer proximity, orientation, context reset, entry interference, next-step availability, AI-referral instrumentation, scoped Core Web Vitals). The build plan's own DoD warning for this stage is worth reading before starting: "If a finding would be identical for a Google visitor, rewrite it or drop it" -- ARRIVE is the stage most at risk of degrading into a generic UX audit, which the plan explicitly calls out as scoring nothing.

## Day 7 — Thu 3 Sep · Stage ⑥ ARRIVE
**Status:** done

- [x] `arrival-engagement-audit` (`skills/arrival-engagement-audit/scripts/arrive_detect.py`): all seven detectors — `ENGAGE-001` answer proximity, `ENGAGE-002` orientation gap, `ENGAGE-003` context reset, `ENGAGE-004` entry interference, `ENGAGE-005` missing next-step, `ENGAGE-006` no AI-referral instrumentation, `ENGAGE-007` scoped response latency (TTFB proxy, the build plan's own cut-list fallback for LCP/INP).
- [x] Groundwork: fixed a real bug in `Chunk.char_offset` (was per-segment, not per-word — every chunk after the first drawn from one large block reported the segment's *start* offset, not its own position) so `ENGAGE-001` has a correct signal to work from; added `page_content_length` and `AnswerabilityMatrixEntry.top_chunk_position_ratio` (Day 6/7 boundary) so ARRIVE reuses stage ④'s own position data instead of re-deriving it.
- [x] `FetchRecord.elapsed_s` added (httpx's `Response.elapsed`, free during stage ①'s own fetch) so `ENGAGE-007` and `ENGAGE-003` need zero new network calls.
- [x] Wired into `run_audit.py` as stage ⑥, gated on stage ④ having actually run (reads its `answerability_matrix` directly, not just budget-gated like EXTRACT/CITE) — `run_retrieve_stage` now also returns the detected `Entity` so ARRIVE doesn't need to re-derive it.
- [x] **DoD met:** unit tests (positive + negative per detector, `tests/test_arrive.py`) plus an end-to-end clean-control fixture (`tests/fixtures/arrival-clean`) confirming all seven detectors stay silent together and the stage is byte-identical across two runs. Wild-corpus check against 2 real sites (docs.python.org, allbirds.com) confirms findings read as genuinely about the AI-referred persona — see Notes — not a generic UX audit; every finding's `impact_mechanism` text explicitly reasons from "deep-linked, mid-task, zero context," not generic UX language.
- [x] `references/taxonomy.md` grown to 33 entries (7 new ENGAGE-00x); `references/composition.md` extended with ARRIVE's own gating story (audits `citable=True` pages from stage ④'s matrix, not a re-derived guess); `arrival-engagement-audit/SKILL.md` Status section written for real.

**Notes:**

*A real entity-detection bug found by ARRIVE's own wild-corpus sweep, in Day-5 code, not Day 7's:* the live allbirds.com check (12-page sample) came back with every `ENGAGE-002` finding quoting the detected brand name as **"Design System"** — obviously wrong. Traced it rather than dismissing it as a fixture quirk: `detect_entity`'s fallback chain, when no page in the sample resolves as the homepage (`find_homepage_url` returns `None`), fell back to guessing from *whichever sampled page's `<title>`/`<h1>` sorted first* — and allbirds.com's sitemap simply never lists `/` itself, so a 12-page stratified sample had no homepage entry at all. The page that sorted first was `https://www.allbirds.com/pages/design-system`, a real, legitimate Shopify internal style-guide page whose own `<title>` is literally "Design System" — taken as the whole audit's brand name purely by alphabetical accident. This had been silently wrong since Day 5 (`detect_entity` shipped then) but was invisible until now: no earlier stage's findings print the entity name directly into evidence text the way `ENGAGE-002` does, so a wrong guess never surfaced as obviously wrong before. Fixed by removing the "guess from some other page's title/h1" fallback entirely and replacing it with a domain-derived name (`https://www.allbirds.com` → "Allbirds") — always available, and unlike an arbitrary subpage, can't be hijacked by unrelated content. Verified against the exact same live crawl sample that exposed the bug (now correctly detects "Allbirds", `source="domain"`), re-ran the full audit end-to-end (ARRIVE findings now correctly read "'Allbirds' appears on the page but not in its first 500 characters" on real product pages), and confirmed no regression across the existing `test_retrieve_stage.py` suite (all still pass — none of the existing tests exercised the no-homepage-in-sample path, which is exactly why this shipped undetected for two days). This is the second time a Day 5 entity-detection assumption (the first was exact-URL-equality for homepage matching) has been wrong in exactly the same spot; worth remembering that `detect_entity`'s fallback chain has now needed hardening twice, and any future change there should specifically stress-test "the homepage isn't in the sample" as its own case, not just "the JSON-LD is missing."

*ARRIVE findings read as genuinely persona-specific, not generic UX, on both live checks:* docs.python.org — `ENGAGE-005` (no CTA phrase on its one citable page) and `ENGAGE-006` (no analytics signature at all); allbirds.com — `ENGAGE-002` (brand not named in product pages' first 500 characters, post-fix) and `ENGAGE-004` (OneTrust consent-banner signature on every citable page). Checked each against the DoD's own test ("would this be identical for a Google visitor?") — `ENGAGE-002`'s framing is specifically about a visitor who skips the nav entirely, which a browsing visitor doesn't; `ENGAGE-004`'s mechanism is specifically about a visitor who arrives already informed and is now blocked from confirming it, not generic annoyance at cookie banners. Both hold up.

*A calibration point recorded honestly, not suppressed:* `ENGAGE-005` fired on docs.python.org's one citable page (a version-index listing) for having no recognizable CTA phrase — technically correct under the fixed phrase list, but "next step availability" is a weaker concept for a documentation site than a commercial one, and the finding shipped at `low` confidence specifically because of this kind of calibration gap, not tuned to look more certain than it is (same honesty pattern as `TRUST-008`). Not treated as a bug to fix — a docs site genuinely has no purchase/contact/signup affordance on that page, so the detector isn't wrong, just describing something lower-stakes than its commercial-site framing implies. Recorded in `taxonomy.md`'s `ENGAGE-005` entry rather than hidden.

*Fixture-scoping, same established pattern, no new issue this time:* `arrival-clean` was built from the start with the Day 5–6 lesson already applied (realistic, substantial, non-repetitive prose; brand name explicit near the top of every page's own main content, not just the nav) — no trafilatura heading-stripping surprise this time, unlike the throwaway debug script used while fixing the char-offset bug (see below).

*Groundwork bug, found before ARRIVE's own detector code was written:* while building `top_chunk_position_ratio`'s underlying data, a synthetic long-page test (a single ~7.5k-char paragraph spanning 3 chunks) showed all 3 chunks reporting `char_offset=0`. Root cause: `chunk_page`'s word-list construction stored each word's *segment's* start offset, not a running per-word offset — correct only when a segment is shorter than one chunk window, which every existing fixture happened to be. Fixed by tracking a running offset incremented per word; verified directly (offsets went from `0, 0, 0` to `0, 2654, 5316`, ratios `0.00, 0.35, 0.70`), regression-tested (`tests/test_chunk.py::test_char_offset_increases_across_chunks_within_one_large_segment`), and confirmed no existing test had depended on the old (buggy) behavior — this bug was latent and untested since whenever `char_offset` was first added, only surfaced by deliberately testing a page shape realistic enough to span multiple chunks from one block.

*Next session should:* start Day 8 (orchestration, falsification, output). `finding-verification` is the last unbuilt skill — all six funnel-stage detectors now exist and are wired up, so Day 8 is purely cross-cutting: reproduction/re-fetch, contradiction search, sample-adequacy, confidence assignment, dedup/merge across stages (one root cause must not emit six findings — worth checking whether `ENGAGE-002`/`ENGAGE-004` ever double-fire with a CITE-stage finding on the same page for a related reason), and the single-file HTML report (funnel diagram, findings grouped by stage, answerability matrix, prioritized action list).

## Day 8 — Fri 4 Sep · Orchestration, falsification, output
**Status:** done

- [x] `finding-verification` (`skills/finding-verification/scripts/verify_findings.py`): reproduction/re-fetch with a different UA (`GPTBot`), sample-adequacy check (CRITICAL only, floored by the audit's own known page count -- see Notes), a narrow EXTRACT-002 contradiction search (microdata/RDFa carrying the same property JSON-LD omits), confidence downgrade, severity recomputation, demotion to `observations`.
- [x] Dedup/merge (`assemble_report.dedup_findings`): exact-duplicate collapse (safety net) plus a known-pair table for cross-stage same-root-cause merges (`REACH-002`/`ENGAGE-003`), keeping the earlier funnel stage as primary.
- [x] Report assembly wired to accept the verified/deduped findings list directly (`assemble_report`'s new `findings=` parameter), falling back to deriving from `stage_results` when omitted.
- [x] Single-file HTML report (`scripts/render_html.py`): funnel diagram with the first-failing stage highlighted, summary stat row, prioritized action list, findings grouped by stage with full evidence/artifacts/suggested actions, observations section, answerability matrix, run manifest. Self-contained (inline CSS, no external assets), light/dark aware, every site-derived string HTML-escaped, every URL scheme-checked before becoming a clickable link (see Notes).
- [x] Markdown executive summary (`scripts/render_markdown.py`): funnel status table, summary counts, prioritized action list, findings by stage (title + one-liner, not the full artifact dump) -- the "read this first" surface, not a second copy of the HTML report's full detail.
- [x] **DoD met, literally:** `python run_audit.py <site>` now writes `report.json` + `report.html` + `report.md` from one command, all three schema-derived from the same validated `AuditReport`. `tests/test_smoke.py::test_one_command_produces_json_html_and_markdown` asserts all three exist and are well-formed. Runtime confirmed well under budget on two real sites (allbirds.com: 25.9s; docs.python.org: 13.5s), both including the new verification pass.
- [x] 154 tests passing (was 132): `tests/test_finding_verification.py` (11), `tests/test_assemble_report.py` (8), plus 3 new RENDER-001 calibration regression tests in `tests/test_render_gap.py` (see Notes).

**Notes:**

*A real severity-calibration bug found while building the falsification pass, not by it:* the very first version of the sample-adequacy check (`checked < 2` for CRITICAL/HIGH findings) immediately demoted `RENDER-001` on `tests/fixtures/js-only-price` -- a fixture whose whole DoD (Day 3) is "the empty-shell page gets flagged critical." Investigating *why* the check fired instead of just loosening it revealed `RENDER-001`'s primary signal had claimed `BlastRadius.SITE_WIDE` (-> `critical`) unconditionally on *every* empty-shell page since Day 3, with a hardcoded `Scope(checked=1, affected=1)` regardless of how many pages were actually in the render sample -- a real, previously-unguarded severity overclaim ("this one page is empty" scored as "your whole site is JS-only"), not a false positive in the new check. Fixed at the source: `render_detect.detect_empty_shell_pages` now aggregates once across the whole render sample and only claims `SITE_WIDE` when >=90% of it is empty (reusing `REACH-001`'s own Day 3 threshold for the identical question), `PAGE_CLASS` otherwise -- one aggregate finding covering every affected URL (not one redundant finding per page), with an honest `Scope(checked=<sample size>, affected=<empty count>)`. This changed `run_retrieve_stage`'s empty-shell-exclusion logic too: it used to identify "which pages are empty" by checking `severity == "critical"` on RENDER-001 findings, which only worked because *every* empty page used to be critical by construction -- now genuinely wrong, since a page-class-severity empty page is still empty. Fixed by having `run_render_stage` return the exact `empty_shell_urls` set explicitly, rather than the caller reverse-engineering it from finding metadata. Regression-tested directly (`test_render_gap.py`'s two new ratio tests) and via the full fixture suite.

*The sample-adequacy floor needed one more refinement after that fix, caught by the same fixture:* even with `RENDER-001` honestly reporting `Scope(checked=1, affected=1)` on the (genuinely) single-page `js-only-price` fixture, a flat "`checked < 2` fails" rule still demoted it -- because a truly 1-page corpus checking "all of it" is a *complete* sample, not an inadequate one; the risky case is a *small slice of a larger, unexamined corpus*, not a small corpus. Fixed by flooring the adequacy threshold at `min(2, total_pages_available)`, where `total_pages_available` is the audit's own stage ① page count, threaded through from `run_audit.py`. Both scenarios are now regression-tested (`test_critical_finding_from_a_genuinely_single_page_corpus_is_not_demoted` vs. `test_critical_finding_from_tiny_sample_of_larger_corpus_is_demoted`) so the distinction can't silently regress either direction.

*Two stale reference docs fixed while touching this area:* `references/severity-model.md`'s Status footer still said the `compute_severity` function itself was "Day 8 work," three days after it actually shipped (Day 3) -- corrected, and the doc now points at what Day 8 actually added (severity *re*computation after a confidence change, in `finding-verification`). `ai-visibility-orchestrator/SKILL.md`'s own "Current stage coverage" note still said "① REACH only, stages ②-⑥ not wired up" -- five days stale, in the entry-point skill a judge reads first. Neither was found by a test; both were caught by re-reading the docs this session's own work touched, the same "don't trust prose because it compiles" discipline as every prior day's review pass.

*HTML report security, taken seriously rather than assumed:* every string embedded in `render_html.py`'s output can originate from the *audited site itself* (evidence text, extracted strings, entity names) -- this is untrusted content written into a real HTML file a human opens in a real browser, the same class of concern as XSS, just against a local static file instead of a served page. Every value goes through `html.escape()`; every artifact/chunk URL additionally goes through a scheme check (`_safe_link`) that only emits a clickable `<a href>` for `http`/`https` and falls back to escaped plain text otherwise -- a `javascript:`-scheme string embedded in a page's own extracted text (or a hand-edited `report.json`) can't become a clickable link. Verified the renderer end-to-end against four diverse fixtures plus two real sites (allbirds.com, docs.python.org) with no crashes, then actually opened one in a browser (not just parsed) to confirm the funnel diagram, severity coloring, and dark-mode auto-detection render as intended.

*Next session should:* start Day 9 (evaluation & precision hardening). The fixture suite and two-site wild sweep this session already exercised are informal versions of Day 9's own DoD (confusion matrix on controls, wild diversity sweep, determinism, no crashes) -- Day 9's job is to make that formal and published (the confusion matrix table in the README), extend the wild sweep back to the full diversity the plan calls for (Shopify, WordPress, single-page portfolio, non-English site -- not just the two sites re-used for convenience this session), and specifically hunt for false positives to prune, per the plan's own "deleting a mediocre rule raises your score."

## Day 9 — Sat 5 Sep · Evaluation & precision hardening
**Status:** done

- [x] Eval harness (`scripts/eval_fixtures.py`, new -- maintainer/CI tooling, not a shipped skill): runs every fixture through the real pipeline, scores findings against a hand-authored ground truth (the same expectations each fixture's own dedicated test already asserts, tabulated in one place), computes precision/recall/FP-rate on controls. Answerability accuracy (a different ground-truth shape -- per-query outcome, not per-finding) scored separately against `retrieval-answerable`.
- [x] **Confusion matrix, published:** 7 fixtures, 2 known-positive cases, 5 controls. **Precision 1.00, recall 1.00, FP-rate on clean controls 0.00** (0/9 certified-clean-stage checks produced a finding). Answerability accuracy 11/12 (92%) on `retrieval-answerable` -- see Notes for the one miss and why it's a known, accepted characteristic, not a bug.
- [x] Wild diversity sweep, run for real through the full Day 8 pipeline (not just Day 1's manual diagnosis): allbirds.com (Shopify), docs.python.org (static docs), stripe.com (SaaS legal/pricing pages), brittanychiang.com (single-page portfolio), theverge.com (news publisher), zalando.de (non-English, currently unreachable from this network -- see Notes). One real crash found and fixed (see Notes); every other run completed cleanly with sensible findings, spot-checked by hand, not just "didn't crash."
- [x] Degradation testing (`tests/test_degradation.py`, new): a sitemap-less site still gets audited (falls back to the homepage, propagates through the full pipeline, not just the crawl stage) and a 5,000-URL sitemap samples correctly-bounded and near-instantly (<2s), deterministically. The other four named degraded conditions (no Playwright, robots-blocked, JS-heavy SPA, no network mid-run) already had dedicated coverage from earlier days -- not re-tested here, cross-referenced in the new file's own docstring instead of duplicated.
- [x] Determinism strengthened from two runs to three, matching the DoD's own literal wording, on the suite's two canonical determinism tests (`test_smoke.py`, and `test_arrive.py`'s full-pipeline-plus-verification check, the most comprehensive one in the suite).
- [x] Rule pack pruned/fixed based on what the harness and sweep actually found (see Notes) -- **no rule deleted outright this pass** (every rule that fired stayed defensible once fixed), but two real precision bugs in already-shipped detectors were found and corrected, which is the same spirit as the plan's "deleting a mediocre rule raises your score": fix or cut, never leave a known false-positive path in place.
- [x] 161 tests passing (was 155): `tests/test_degradation.py` (3, new file), plus regression tests for both fixed bugs (`test_reach_detectors.py` +2, `test_arrive.py` +1).

**Notes:**

*A real false positive found by the eval harness on its first run:* `REACH-001` fired on the `robots-restricted` fixture -- a compliance fixture (Day 3) never meant to trigger any REACH-00x defect, just to prove the crawler itself respects `Disallow`. Traced it: the fixture's `User-agent: *` rule excludes one low-value page (a staff directory) from *every* crawler equally -- Googlebot, browsers, AI bots alike -- and `disallowed_ai_uas()` correctly (if misleadingly) reports all 12 named bots as "blocked" from that URL, since none of them have a more specific override. The title made it worse: "robots.txt disallows 12 of 12 named AI crawlers" reads as a sweeping site-wide claim despite affecting only 1 of 2 sampled pages. First instinct was a page-count floor (`pages_affected >= 2`) -- reverted immediately on realizing it would silently break an already-established, already-tested case (`test_ai_ua_block_path_scoped_rule_is_not_site_wide_even_if_all_bots_blocked`: 12 *named-bot-only* `Disallow: /pricing` rules, no `User-agent: *` at all, correctly expected to still fire even though only one page is affected -- REACH-002's own worked example in `severity-model.md` already establishes that a single high-value page can legitimately warrant a real finding). The actual mechanism gap: REACH-001 never distinguished *AI-specific* discrimination (named-UA rules, or a wildcard rule with a more-permissive AI-bot override) from a *generic* exclusion that just happens to also catch AI bots as a side effect of blocking everyone. Fixed by checking, per URL, whether a generic non-AI crawler (`GENERIC_CRAWLER_UA`, a real Googlebot UA string, new in `crawl.py`) would *also* be blocked -- if so, the exclusion isn't AI-targeted and doesn't count. Also fixed the title/evidence to always state the affected-page fraction, not just bot coverage, regardless of whether the finding still fires. Both the false-positive case and the must-still-fire contrast case are now regression-tested side by side.

*A real crash found by the wild sweep, not the harness:* `zalando.de` raised a pydantic `ValidationError` (`Finding.artifacts` needs >=1 item, got 0) mid-run. Traced to `arrive_detect.detect_no_ai_referral_instrumentation`: `robots.txt` fetch failed entirely for this site from this network right now (confirmed directly -- `fetch_robots` returned `fetched=False`), sitemap discovery correctly fell back to `[base_url]` per its own documented behavior, and that one page also failed to fetch, leaving `run_arrive_stage`'s `all_pages` genuinely empty. Every other ARRIVE detector already had an explicit guard for this (`if not X: return None` before building artifacts from a list comprehension) or took a single URL directly rather than an aggregate collection -- `detect_no_ai_referral_instrumentation` was the one that predates ARRIVE's own existence in spirit (its "scan everything, return a Finding unconditionally after the loop" shape is the exact bug `retrieve_detect.run_retrieval_simulation` already hit and fixed on Day 5, just never ported to this later detector). Fixed with the same one-line guard, regression-tested directly (`detect_no_ai_referral_instrumentation({}) is None`) and re-confirmed against the live site (now degrades gracefully: `0 findings, 0/1 sampled pages reachable`, no crash). `zalando.de`'s own current unreachability is a real, separately-confirmed network/site condition, not a pipeline bug -- Day 1's research characterized it as reachable-but-empty-robots-txt; sites' access policies and this session's network path both change over time, and the interesting part for Day 9's purposes was always the empty-corpus code path this happened to exercise, not zalando.de specifically.

*One real miss, investigated and deliberately left alone:* the `retrieval-answerable` fixture's contact-intent query "What is Rowan Cast Iron Co.'s customer support?" scores `partial`, not `answerable` -- the page says "reach the workshop... call us... during business hours," never the words "customer" or "support." Tried swapping the template to "How can I reach {brand} by phone or email?" -- made it *worse* (scored `ungrounded`, since the fixture's copy also never literally says "phone" or "email"). Reverted immediately: this isn't a bad template, it's the deterministic BM25-only retriever correctly, honestly reporting that the query's literal terms don't overlap the page's literal terms, which is exactly the documented, accepted tradeoff of a lexical-only retriever (build plan Part 4: BM25 as "a conservative floor," not a semantic matcher) -- chasing it further with different synonyms would only relocate the same miss to a different query on this same fixture, and tuning a template to match one fixture's specific writing style is the overfitting the project's own discipline explicitly forbids. Left as an honestly-reported 92%, not silently hidden or chased into a change that doesn't generalize.

*Wild sweep spot-checks, for the record:* stripe.com's 18 findings (6 REACH-002 locale-redirects on `/legal/...` pages with corroborating empty-body evidence, 6 matching EXTRACT-003 no-`<h1>` findings on those same redirect-affected pages, plus CHUNK-003/TRUST-008/ENGAGE-001/002/006) all read as coherent and mechanism-consistent on manual review, zero findings judged wrong. theverge.com now blocks 9 of 12 named AI bots site-wide (`medium`, correctly not `critical` since bot coverage is 75%, under the 90% threshold) -- a real change from Day 1's "open robots" characterization, consistent with robots.txt policies changing over nine in-story months, not a detector bug.

*Honest gap:* no WordPress-powered local-business site in this session's sweep (the build plan names it explicitly). Not chased further given the rest of the diversity already covered (SaaS, Shopify, docs, portfolio, news, non-English-attempted) and the two real bugs already found were higher-value uses of the remaining time than adding an eighth site of a shape the pipeline has no particular reason to handle differently.

*Next session should:* start Day 10 (package & narrative). The README is still Day 2's placeholder by design (says so explicitly) -- Day 10 writes the real one: the funnel thesis, the composition story, and this day's actual published confusion matrix (precision 1.00, recall 1.00, FP-rate 0.00 on controls) rather than a promise to publish one later. Also due: trim every `SKILL.md` under 500 lines with detail pushed to `references/` (not yet checked this session), the full `skills-ref validate` + zip-size + clean-container run-through, and a committed sample report from a real site.

## Day 10 — Sun 6 Sep · Package & narrative
**Status:** not started

**DoD:** zip built, unzipped in a clean container, run end-to-end from the README instructions alone.
